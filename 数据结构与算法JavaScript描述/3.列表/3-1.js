/**
 * 列表的完整抽象数据类型定义
 */

 class List{
     listSize;
     position;
     length;
     clear();
     toString();
     getElement();
     insert();
     append();
     remove();
     front();
     end();
     prev();
     next();
     current();
     moveTo();
 }